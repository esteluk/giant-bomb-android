package harris.GiantBomb;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class StringUtils {
	
	public static String removeEmbeds(String in) {
		// doesn't work
		//return in.replaceAll("\\<embed.*?embed\\>", "<i>Video Redacted</i>");
		return in;
	}

}
